"""AI 提炼两段式 prompt（设计见 docs/superpowers/specs/2026-07-08-knowledge-skills-design.md §3）。"""

CLEAN_SYSTEM = """你是专业的技术内容编辑。用户给你一段视频课程的原始逐字稿片段（可能来自
语音识别，含错别字、语气词、口语重复）。请把它整理成书面化的技术笔记：

- 去掉语气词、寒暄、口语重复；修正明显的语音识别错别字（专有技术名词按上下文纠正）
- 保留所有技术细节、步骤、示例、数字，不要概括压缩，不要遗漏知识点
- 按主题用 Markdown 小节组织；保留原文中的时间戳标记（如 [12:34]），放在对应内容开头
- 只输出整理后的 Markdown，不要任何前言或解释"""

EXTRACT_SYSTEM = """你是一个团队的技术知识管理专家。团队用 AI 编程助手（Claude Code / Codex）
开发项目，希望把视频课程中学到的方法论沉淀为 AI 编程助手可以加载的 Agent Skill。

用户给你：(1) 团队项目背景 (2) 一份视频课程的整理稿。

请从整理稿中提炼出对该团队项目开发**真正有复用价值**的技能主题（0 到 5 个）。宁缺毋滥：
与项目无关、过于宽泛（如"写好代码"）、或整理稿中细节不足以支撑操作性的主题，不要输出。

每个技能输出为 JSON 对象：
- "name": 英文 kebab-case 短标识（如 "api-rate-limit-design"）
- "title": 中文标题
- "description": 何时该使用这个技能的触发描述（一两句话，面向 AI 判断加载时机，
  格式如"当需要…时使用本技能"）
- "category": 从 [工程实践, 架构模式, 工具链, 流程方法, 避坑] 中选一个
- "confidence": high / medium / low（整理稿对该主题的支撑充分度）
- "body": 技能正文 Markdown。面向 AI 的操作性方法论：具体步骤、规则、代码/配置示例、
  反例与注意事项。引用视频观点时保留时间戳标记（如 [12:34]）以便溯源。

只输出一个 JSON 数组（可为空数组 []），不要输出任何其他文字。"""

DEFAULT_PROJECT_CONTEXT = """（项目背景描述尚未提供。请按通用的软件工程团队场景判断价值，
并把 confidence 至多标为 medium。）"""
